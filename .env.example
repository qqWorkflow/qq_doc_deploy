# ===========================================
# QQ Doc Server - Configuration
# ===========================================
#
# 1. Copy this file as .env:
#    cp .env.example .env
#
# 2. Create folder for SSH keys:
#    mkdir -p .ssh
#
# 3. Generate deploy key:
#    ssh-keygen -t ed25519 -f .ssh/deploy_key -N "" -C "qq_doc deploy key"
#
# 4. Add public key to GitHub:
#    cat .ssh/deploy_key.pub
#    → GitHub repo → Settings → Deploy keys → Add deploy key
#    → DO NOT check "Allow write access" (read-only)
#
# 5. Start:
#    docker-compose up -d
#
# ===========================================

# Git repository URL (SSH format)
GIT_REPO=git@github.com:Tybeos/qq_doc.git

# Path to SSH private key (relative to docker-compose.yml)
SSH_KEY_PATH=./.ssh/deploy_key

# ===========================================
# Optional settings (defaults shown)
# ===========================================

# Git branch to sync
# GIT_BRANCH=main

# Sync interval (how often to pull from git)
# SYNC_PERIOD=60s

# HTTP port for nginx
# HTTP_PORT=80
